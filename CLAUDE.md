# CLAUDE.md

## Preferenze di workflow Git

- **Non creare pull request.** Committare direttamente su `main` e fare sempre `git push` su `main`.
- Dopo ogni modifica completata: `git add` → `git commit` con messaggio chiaro → `git push origin main`.
